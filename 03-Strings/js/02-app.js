//METODOS PARA LOS STRINGS

const producto = "Monitor 20 Pulgadas";

//método que no utiliza parentesis, es una propiedad
console.log(producto.length);

//saber en que pocision está en mi string
console.log(producto.lastIndexOf('Pulgadas'));

//para saber si esta o no esta el string
console.log(producto.includes('Monitor'));
