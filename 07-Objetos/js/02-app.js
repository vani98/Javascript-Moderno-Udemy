const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
    disponible: true
};
console.log(producto);

//Acceder al nombre
    //Sintaxis de punto (mas comun)
    console.log(producto.nombre);

    //Otra forma
    console.log(producto['nombre']);